---
phase: 17-deploy-configure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [DEPLOY-01, DEPLOY-02, DEPLOY-03]

must_haves:
  truths:
    - "Railway deployment is running the latest GitHub commit (v1.1 code with T1 fix, battle-test endpoint, reminders)"
    - "APP_ENV is set to production, CAT_EMAIL is set to Cat's real email, REDIS_URL is connected"
    - "Health endpoint returns 200 OK with killSwitch: false"
    - "Gmail poller is actively running with no startup errors"
  artifacts:
    - path: "Railway deployment"
      provides: "Running production instance"
    - path: "Railway environment variables"
      provides: "Correct production configuration"
  key_links:
    - from: "GitHub main branch"
      to: "Railway deployment"
      via: "Auto-deploy on push"
    - from: "Railway env vars"
      to: "Application behavior"
      via: "APP_ENV=production disables dev overrides"
---

<objective>
Deploy the latest v1.1 code to Railway and verify all services are healthy.

Purpose: The v1.1 codebase (Phases 12-16) has been built and tested locally but is not yet running in production. This plan pushes the code, configures production environment variables, and verifies the deployment is healthy.

Output: A running Railway deployment with correct env vars, healthy endpoints, and active Gmail polling.
</objective>

<execution_context>
@C:/Users/lucac/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lucac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key facts from project history:
- Railway URL: doc-automation-production.up.railway.app
- GitHub repo: VentureMortgages/Mortgage-Automation (main branch)
- Railway auto-deploys from GitHub main branch
- Railway CLI has file-locking issues on Windows -- do NOT use `railway up`
- Health endpoint: GET /health (returns { status: "ok", killSwitch: false })
- Gmail poller: intake worker polls docs@venturemortgages.com every 120 seconds
- Kill switch: AUTOMATION_KILL_SWITCH env var (should be unset or "false")
- CAT_EMAIL: used by reminders/notify-cat.ts (falls back to docs@venturemortgages.com if unset)
- APP_ENV: defaults to "development" -- must be explicitly set to "production"
- Current Railway env: APP_ENV was "development" as of last check (needs updating)
- GOOGLE_REFRESH_TOKEN: NOT SET on Railway (uses service account -- correct)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push latest code to GitHub and trigger Railway deploy</name>
  <files></files>
  <action>
1. Check if local main branch is ahead of origin/main:
   ```
   git status
   git log origin/main..HEAD --oneline
   ```
2. If there are unpushed commits, push to GitHub:
   ```
   git push origin main
   ```
   Railway auto-deploys from main, so this triggers a new deployment.

3. If local is already up-to-date with origin, verify the latest commit on GitHub matches the local HEAD (commit fb4f522 or later containing the T1 naming fix and battle-test endpoint from commit c841ed7).

4. Wait ~60 seconds for Railway to pick up the deploy, then check deployment status.
  </action>
  <verify>
    <automated>git log origin/main --oneline -1</automated>
  </verify>
  <done>GitHub main branch contains commits c841ed7 (T1 fix + battle-test) and fb4f522 (reminder formatting fix) or later. Railway has started deploying.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify and set Railway environment variables</name>
  <files></files>
  <action>
User opens Railway dashboard and verifies/sets environment variables for production.
This is a human-interactive task -- Claude cannot access the Railway dashboard directly.

What was built: Code pushed to GitHub, Railway deploying. Now need to verify env vars are correct for production.

How to verify:
Open the Railway dashboard (https://railway.app) and navigate to the doc-automation-production service. Go to the Variables tab.

**MUST SET/CHANGE:**
1. `APP_ENV` = `production` (currently "development" -- MUST change)
   - This controls email recipient overrides, logging level, and other dev-mode behaviors
2. `CAT_EMAIL` = Cat's actual email address (ask Taylor if unknown)
   - Used by the reminder system to send "Follow up: Need docs" notifications
   - If not set, falls back to docs@venturemortgages.com

**MUST VERIFY PRESENT (do not change):**
3. `REDIS_URL` -- should be set automatically by Railway's Redis plugin
4. `GOOGLE_SERVICE_ACCOUNT_KEY` -- base64-encoded service account JSON
5. `FINMO_API_KEY` -- Finmo API bearer token
6. `GHL_API_KEY` -- GoHighLevel/MyBrokerPro API key
7. `GHL_LOCATION_ID` -- should be `bzzWH2mLpCr7HHulO3bW`
8. `DRIVE_ROOT_FOLDER_ID` -- should be `1g6UIKA5hk1oNSotiTA89z2m65yNIBTn0`
9. `DOC_INBOX` -- should be `docs@venturemortgages.com`
10. `EMAIL_SENDER` -- should be `dev@venturemortgages.com`
11. `EMAIL_BCC` -- should be `docs@venturemortgages.com`

**MUST VERIFY ABSENT or FALSE:**
12. `AUTOMATION_KILL_SWITCH` -- should be unset or NOT "true"
13. `GOOGLE_REFRESH_TOKEN` -- should be unset (service account mode)
14. `BUDGET_SHEET_ENABLED` -- should be unset (auth broken, not needed for go-live)
  </action>
  <verify>
    <automated>curl -s https://doc-automation-production.up.railway.app/health</automated>
  </verify>
  <done>APP_ENV=production is set, CAT_EMAIL is set, all required env vars are present, kill switch is OFF. User has confirmed all changes in Railway dashboard.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify deployment health and services</name>
  <files></files>
  <action>
User checks Railway deployment logs and health endpoint to confirm everything is running correctly.
This is a human-interactive task -- Claude cannot access Railway logs directly.

What was built: Code deployed and env vars configured. Now verify the deployment is healthy and all services are connected.

How to verify:

**Step 1 -- Health endpoint:**
Open in browser or run:
```
curl https://doc-automation-production.up.railway.app/health
```
Expected response: `{ "status": "ok", "killSwitch": false }`

**Step 2 -- Check Railway deploy logs:**
In Railway dashboard, go to the Deployments tab for doc-automation-production.
Click on the latest deployment and check the logs:
1. Look for: "Server listening on port XXXX" (startup success)
2. Look for: "Intake worker started" or "Polling docs@" (Gmail poller active)
3. Look for: No red error lines at startup (no missing env vars, no Redis connection failures)
4. Specifically verify NO errors mentioning: REDIS_URL, GOOGLE_SERVICE_ACCOUNT_KEY, GHL_API_KEY, FINMO_API_KEY

**Step 3 -- Verify kill switch is OFF:**
The /health response should show `killSwitch: false`. If it shows `true`, unset or remove the `AUTOMATION_KILL_SWITCH` env var in Railway.
  </action>
  <verify>
    <automated>curl -s https://doc-automation-production.up.railway.app/health | grep -q '"status":"ok"' && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Health endpoint returns 200 OK with killSwitch=false. Railway logs show server listening, Gmail poller active, and no startup errors. All services (Redis, Gmail, Drive, CRM, Finmo) are connected.</done>
</task>

</tasks>

<verification>
Phase 17 is complete when:
1. `curl https://doc-automation-production.up.railway.app/health` returns `{"status":"ok","killSwitch":false}`
2. Railway deployment is running the commit that includes T1 naming fix (c841ed7) and reminder formatting fix (fb4f522)
3. Railway logs show the Gmail intake worker polling without errors
4. APP_ENV=production is set in Railway env vars
</verification>

<success_criteria>
- Health endpoint returns 200 OK with killSwitch=false
- APP_ENV=production (not development)
- CAT_EMAIL is set to Cat's actual email address
- Gmail poller is actively running (visible in Railway logs)
- No startup errors in Railway deployment logs
- All required env vars (REDIS_URL, GOOGLE_SERVICE_ACCOUNT_KEY, GHL_API_KEY, FINMO_API_KEY, DRIVE_ROOT_FOLDER_ID) are present
</success_criteria>

<output>
After completion, create `.planning/phases/17-deploy-configure/17-01-SUMMARY.md`
</output>
