---
phase: production-readiness
task: deploy-and-battle-test
status: in_progress
last_updated: 2026-03-04T01:42:59.959Z
---

<current_state>
Code is committed and pushed to GitHub (c841ed7) but NOT yet deployed to Railway.
Railway CLI `up` command has file-locking issues on Windows — need to deploy via Railway dashboard or fix CLI.
Two features ready: T1 naming fix + battle-test intake endpoint.
</current_state>

<completed_work>

- T1 naming fix: Strip institution/amount from T1 filenames. "Simran - T1 Dart Bryant LLP $4.3k" -> "Simran - T1 2023". Changed naming.ts, classifier.ts prompt, added 2 test cases. All 897 tests passing.
- Battle-test endpoint built: POST /admin/test-intake (synchronous pipeline trace with dryRun mode) + GET /admin/recent-messages (list inbox messages). New file: src/admin/test-intake.ts, wired into server.ts.
- Full pipeline analysis: Mapped complete doc intake flow end-to-end (email -> classify -> match -> file -> CRM update)
- Contact matching analysis: Traced how forwarded docs match to contacts (primary signal: borrower name from PDF via Gemini)
- APP_ENV impact analysis: Documented every dev vs production difference (email routing, subject prefix, impersonation, CRM naming)
- CRM-Drive folder mapping: Documented how contacts link to Drive folders, backfill script exists
- 10 scenario verification: Verified each "day in the life" scenario against actual code, noted corrections
- Railway log investigation: Found "entity not found" errors + Drive filing failures — root cause is APP_ENV=development on Railway

</completed_work>

<remaining_work>

1. **DEPLOY** new code to Railway (dashboard or fix CLI file-lock issue)
2. **Battle-test** with real messages using /admin/test-intake endpoint
3. **Set APP_ENV=production** on Railway (fixes Gmail/Drive impersonation mismatches, "entity not found" errors, Drive filing failures)
4. **Set CAT_EMAIL** env var on Railway for reminder notifications
5. **Run backfill script** (npx tsx scripts/backfill-drive-links.ts) to link existing CRM contacts to Drive folders
6. **Reply to Cat** with testing instructions + acknowledge T1 bug (fixed)
7. **Clean up [TEST] contacts/opps** in MyBrokerPro before production
8. **SPF/DKIM/DMARC** — Taylor needs to configure on venturemortgages.com
9. **Fix Finmo link on website** — still points to ILMB team, not Venture Mortgages

</remaining_work>

<decisions_made>

- T1 naming: Skip institution AND amount for T1 docs (personal tax returns only need year). Also updated Gemini prompt to not extract these fields.
- Battle-test endpoint: Built as synchronous inline pipeline (not BullMQ queue) so it returns immediate JSON trace. dryRun=true by default for safety.
- Domain filter kept: External senders (non @venturemortgages.com) are intentionally blocked. Cat must forward docs — this prevents random external emails from creating CRM contacts.
- Contact matching for forwards: Primary signal is borrower name extracted from PDF by Gemini. Works well for unique names (0.85 confidence -> auto-file). Ambiguous names route to Needs Review for Cat.

</decisions_made>

<blockers>

- Railway CLI `up` command fails with "The process cannot access the file because another process has locked a portion of the file" on Windows. Workaround: deploy via Railway dashboard.
- APP_ENV=development on Railway causes: Gmail impersonation mismatch, Drive folder access failures, "entity not found" errors. Must switch to production.
- Drive folder ID 1pVRRWSDbqvV2BB6nursO9yyYRAXvN5G1 is stale/inaccessible — causing filing failures in production logs.

</blockers>

<context>
Cat reported two production issues: (1) T1 named wrong (FIXED in code, not deployed), (2) forwarded email not processed (likely caused by APP_ENV mismatch). She also asked for testing instructions.

The system is fully built end-to-end (phases 1-16 complete, 897 tests). The gap is deployment configuration — APP_ENV must be production for Gmail/Drive to work correctly. The battle-test endpoint will let us verify the pipeline works before going live.

Scenario verification found corrections needed: person subfolder routing (no separate Income/ folder — person name IS the subfolder), co-borrower routing goes to client folder not deal subfolder for non-property docs, auto-create requires both first AND last name, encrypted PDF handling has no special detection.
</context>

<next_action>
1. Deploy code to Railway (use dashboard at railway.app -> Venture Mortgages -> doc-automation -> trigger deploy from latest GitHub commit c841ed7)
2. Test: curl https://doc-automation-production.up.railway.app/admin/recent-messages
3. Pick a message with attachments, run: curl -X POST .../admin/test-intake -H "Content-Type: application/json" -d '{"messageId":"PICK_ONE"}'
4. Review the trace output to verify classification + matching + filing logic
</next_action>
